# Em produção o frontend e o backend ficam atrás do mesmo Application Load
# Balancer, que roteia /usuarios*, /auth*, /documentos* etc. diretamente para
# o backend. Por isso a base da API é vazia (sem o prefixo /api usado em dev).
VITE_API_URL=
